<!DOCTYPE html>
<html>
  <head>
    <title>FinancialHealth</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload"  %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://unpkg.com/flowbite@1.5.1/dist/flowbite.min.css" %>
    
    <%= javascript_importmap_tags %>
  </head>

  <body>
    
    <main class="xl:container mx-auto h-auto mt-8 flex bg-white dark:bg-gray-900">
      <p class="notice "><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <% if user_signed_in? %>
        <%= render partial: 'partials/sidebar' %>
      <% else %>
        <%= link_to 'Login', new_user_session_path %>
        <%= link_to 'Create Account', new_user_registration_path%>
      <% end %>
      
      <%= yield %>
    </main>
  </body>
</html>
